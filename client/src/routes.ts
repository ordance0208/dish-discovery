const AUTH_PATHS = {
  LOGIN: '/login',
  REGISTER: '/register',
};

const PATHS = {
  HOME: '/home',
  RECIPES: '/recipes',
  RECIPE_SUBMIT: '/recipe-submit',
  ABOUT: '/about',
  PROFILE: '/profile',
  PROFILE_SETTINGS: '/profile-settings'
};

export { AUTH_PATHS, PATHS };
