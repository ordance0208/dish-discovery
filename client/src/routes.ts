const AUTH_PATHS = {
  LOGIN: '/login',
  REGISTER: '/register',
};

const PATHS = {
  HOME: '/home',
  RECIPES: '/recipes',
  ABOUT: '/about',
  PROFILE_SETTINGS: '/profile-settings'
};

export { AUTH_PATHS, PATHS };
